---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: intel-gpu-device-plugin
  namespace: intel-devices
spec:
  interval: 10m
  timeout: 5m
  chart:
    spec:
      chart: intel-device-plugins-gpu
      version: 0.35.0
      sourceRef:
        kind: HelmRepository
        name: intel
        namespace: intel-devices
  values:
    image:
      tag: 0.34.0
