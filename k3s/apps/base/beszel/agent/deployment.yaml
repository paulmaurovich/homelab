---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: beszel-agent
  namespace: beszel
spec:
  selector:
    matchLabels:
      app: beszel-agent
  template:
    metadata:
      labels:
        app: beszel-agent
    spec:
      hostNetwork: true
      containers:
        - name: beszel-agent
          image: ghcr.io/henrygd/beszel/beszel-agent:0.18.5@sha256:2c98ad71778344d417b9b54d1d415542b28539537c6db8ac64fd6e25196e6516
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 45876
              hostPort: 45876
          envFrom:
            - configMapRef:
                name: beszel-agent-config
            - secretRef:
                name: beszel-agent-secrets
      restartPolicy: Always
      tolerations:
        - effect: NoSchedule
          key: node-role.kubernetes.io/master
          operator: Exists
        - effect: NoSchedule
          key: node-role.kubernetes.io/control-plane
          operator: Exists
        - effect: NoSchedule
          key: node-role.kubernetes.io/postgres
          operator: Exists
  updateStrategy:
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 100%
    type: RollingUpdate
