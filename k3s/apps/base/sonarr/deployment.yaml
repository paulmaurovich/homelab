---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sonarr
  namespace: sonarr
spec:
  selector:
    matchLabels:
      app: sonarr
  template:
    metadata:
      labels:
        app: sonarr
    spec:
      containers:
        - name: sonarr
          image: linuxserver/sonarr:version-4.0.16.2944@sha256:37be832b78548e3f55f69c45b50e3b14d18df1b6def2a4994258217e67efb1a1
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8989
          envFrom:
            - configMapRef:
                name: sonarr-config
            - secretRef:
                name: sonarr-db-secrets
          volumeMounts:
            - name: sonarr-data
              mountPath: /config
            - name: sonarr-tvshows
              mountPath: /tvshows
            - name: sonarr-downloads
              mountPath: /downloads
      volumes:
        - name: sonarr-data
          persistentVolumeClaim:
            claimName: sonarr-data
        - name: sonarr-tvshows
          persistentVolumeClaim:
            claimName: sonarr-tvshows
        - name: sonarr-downloads
          persistentVolumeClaim:
            claimName: sonarr-downloads
